name: Pipeline
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Entrando na pasta
        run: pwd
        
      - name: saindo da pasta
        run: cd ..

      - name: olhando
        run : pwd

      - name: entrando
        run: cd fast_zero
